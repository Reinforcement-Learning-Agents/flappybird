# flappybird
This project implements a reinforcement learning agent for Flappy Bird, a control task with sparse rewards and strict timing constraints. The environment is modeled as an MDP where the agent learns when to flap to avoid obstacles and maximize survival time.
